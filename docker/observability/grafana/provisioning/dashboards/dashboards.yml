apiVersion: 1

providers:
  - name: ExperimentHub
    folder: ExperimentHub
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
